## 1️⃣ What is the difference between `var`, `let`, and `const`?

**Ans:**  
`var` is the old way to declare a variable in JavaScript. It is function scoped and hoisted. That's why it can create confusion sometimes. Developers avoid it now.

`let` is a modern way to declare a variable which can change later.

`const` is used to declare a variable that can't change later.

---

## 2️⃣ What is the spread operator (`...`)?

**Ans:**  
The spread operator unpacks values from an array or object. It's useful when there is a need to copy, combine, or merge an array or object without changing the original data.

---

## 3️⃣ What is the difference between `map()`, `filter()`, and `forEach()`?

**Ans:**  
All three are used to work with arrays, but they behave a bit differently.

- **`forEach()`** simply loops through every item in the array and runs a function on each one. It doesn’t return anything.
- **`map()`** also loops through the array, but it transforms each item and returns a brand-new array with the updated values.
- **`filter()`** checks each item against a condition and returns a new array containing only the items that pass that test.

---

## 4️⃣ What is an arrow function?

**Ans:**  
An arrow function is a shorter way to write functions using the `=>` syntax.

---

## 5️⃣ What are template literals?

**Ans:**  
Template literals are strings written using backticks (`` ` ``) instead of single or double quotes. They make it easy to insert variables directly into a string using `${variable}`.